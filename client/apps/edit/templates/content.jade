section#edit-content
  #edit-title
    .edit-required
    input.invisible-input(
      placeholder='Type a title'
      value=article.get('title')
    )
  #edit-lead-paragraph.edit-body-container
    input.invisible-input( placeholder='Lead paragraph (optional)' value=article.get('lead_paragraph') )
  #edit-author-section
    p= user.get('name')
    p Staff Writer
    p= moment(article.get('created_at') || moment()).format('MMM D, YYYY h:mm a')
  .edit-body-container
  #edit-sections.edit-body-container