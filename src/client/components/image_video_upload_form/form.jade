section.image-upload-form.image-upload-form--video( data-state=(src ? 'loaded': '') )
  h1
    | Drag &&nbsp;
    span.image-upload-form-drop Drop&nbsp;
    | or&nbsp;
    span.image-upload-form-click click&nbsp;
    | to upload
  h2 Up to 10MB
  .type-error Please choose .png, .jpg, .gif, or .mp4
  .size-error File is too large
  if src && src.indexOf('mp4') == -1
    .image-upload-form-preview( style="background-image: url(#{resize(src,{width: 800})})" )
  else if src
    video.image-upload-form-preview
      source(src=src)
  else
    .image-upload-form-preview
  .image-upload-form-remove ×
  input.image-upload-form-input( type='file' accept='.png,.gif,.jpeg,.mp4')
  input.image-upload-hidden-input( type='hidden' name=name value=src )
  .image-upload-form-progress-container
    .image-upload-form-progress
