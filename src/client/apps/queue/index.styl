@import '../../components/stylus_lib'

.queue-root-container
  &[data-state='scheduled']
    .queue-scheduled
      display block
    .queue-queued, .queue-filter-search
      display none
  &[data-state='daily_email'], &[data-state='weekly_email']
    .queue-scheduled
      display none
    .queue-queued, .queue-filter-search
      display block
  &[data-loading='true']
    .loading-spinner
      display block
    .queue-loading-container
      position fixed
      top 0
      left sidebar-width
      width "calc(100% - %s)" % sidebar-width
      height 100%
      z-index 4
      background rgba(255,255,255,0.8)
  &[data-loading='false']
    .loading-spinner
      display none

  .page-header
    .max-width-container
      display flex
      justify-content space-between
      align-items center
.queue-queued
  margin 60px auto 120px auto
  .queued-articles
    &__header-container
      display flex
      border-bottom 1px solid gray-lighter-color
      padding-bottom 20px
      justify-content space-between
    &__header-text
      garamond('headline')

  .article-list
    &__checkcircle
      svg
        fill-color black
      &:hover
        fill-color gray-color
